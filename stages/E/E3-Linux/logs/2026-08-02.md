# E3 Linux 学习日志

日期：2026-08-02  
学习内容：Missing Semester 第四讲 Data Wrangling

## 今日完成

- 学习 `grep` 的筛选和正则匹配。
- 使用 `sort`、`uniq`、`wc` 完成排序、去重和计数。
- 学习 `sed` 的替换、捕获组和原地修改。
- 学习 `awk` 的按列处理、求和、平均值和分组统计。
- 练习常用正则表达式。
- 完成单词筛选、词尾统计和启动时间统计。

## 练习结果

- 至少包含 3 个 `a` 且不以 `'s` 结尾的单词：854 个。
- 最近可用的 5 次启动时间：
  - 平均值：14.978 s
  - 中位数：16.110 s
  - 最大值：16.423 s

## 遇到的问题

- `sed` 使用捕获组时忘记添加 `-E`。
- 两字母组合脚本最开始生成不完整。
- 最近三次启动日志比较题较难，暂时跳过。

## 今日收获

```text
grep  筛选
sed   替换和提取
awk   计算和统计
sort  排序
uniq  去重和计数
wc    统计数量
```

复杂命令不需要全部背下来，应拆开执行并逐步检查结果。

## 下一步

学习进程、信号、前后台任务、`jobs`、`bg`、`fg` 和 `kill`。
