# 2026-06-27 E1 C语言自测

## 今日目标

开始一生一芯 E 阶段，进入 E1 C语言程序设计部分。

由于已经掌握 C 语言基本语法，并且当前也在学习 C++，所以今天不从零基础语法开始，而是先进行一次 C 语言能力自测，判断后续重点补哪些内容。

## 今日完成

完成了前三个 C 语言自测程序：

- `01_string_basic.c`
- `02_pointer_swap.c`
- `03_recursion.c`

其中：

- 字符串函数基本能写出
- 指针交换变量没有问题
- 递归函数基本能理解和实现

## 暴露的问题

单链表完整实现不熟，尤其是：

- 链表节点的连接关系
- `struct Node *` 的使用
- `struct Node **` 的使用
- 插入和删除节点时头指针的变化
- `malloc` 和 `free` 的配合使用

## 今日结论

C 语言基本语法可以快速跳过，后续重点补：

- 指针
- 结构体
- 链表
- 动态内存管理

## 下一步计划

下一次重点学习单链表，从创建节点、尾插、遍历、删除节点、释放链表开始。

建议新建代码文件：

```text
code/c/E1/04_linked_list.c
```

重点实现：

```c
struct Node *create_node(int data);
void push_back(struct Node **head, int data);
void print_list(struct Node *head);
void delete_value(struct Node **head, int value);
void free_list(struct Node *head);
```
